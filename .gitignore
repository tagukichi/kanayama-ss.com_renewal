node_modules/
*.tmp.mjs
.DS_Store
Thumbs.db

# メディア本体は git に入れない方針（docs/asset-handoff.md 参照）
# 一時的に置く場合に備えた保険
assets/wp-uploads/

# 検証用に playwright を入れたときの残骸（ビルド自体は依存パッケージ0）
package.json
package-lock.json

# テーマのプレビュー出力とZIP（生成物）
theme-preview/
dist/
